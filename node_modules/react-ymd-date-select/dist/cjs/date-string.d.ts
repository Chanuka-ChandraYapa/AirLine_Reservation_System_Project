export declare function compileDateString(year: number, month: number, day: number): string | null;
export declare function parseDateString(dateString: string): {
    year: string;
    month: string;
    day: string;
};
export declare function getDateString(date: Date): string | null;
