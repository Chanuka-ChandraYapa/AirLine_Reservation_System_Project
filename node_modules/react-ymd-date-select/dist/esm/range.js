export function range(first, last) {
    if (first > last) {
        return range(last, first).reverse();
    }
    const arr = [];
    for (let i = first; i <= last; i++) {
        arr.push(i);
    }
    return arr;
}
